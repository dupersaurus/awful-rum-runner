using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UI {
	public class PurchaseItem : MonoBehaviour {

		[SerializeField]
		private RectTransform _nameLabel;

		[SerializeField]
		private RectTransform _countLabel;

		[SerializeField]
		private RectTransform _priceLabel;

		// Use this for initialization
		void Start () {
			
		}
		
		// Update is called once per frame
		void Update () {
			
		}
	}
}
